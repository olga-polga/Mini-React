var BetterButton  = require('../../src/main/js/components/BetterButton');
describe('a passing test', () => {
    it('should pass', () => {
        expect(true).to.be.true;
    });
});
describe('BetterButton', () => {
    it('should do whatever', () => {
        // our tests
    });
})